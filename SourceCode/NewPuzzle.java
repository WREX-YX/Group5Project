if level == hard
  for (int i =0, i<15,i++){
    random number generator for grid.unusedSpot
    grid.fillBox()
  }
if level ==medium
  for (int i =0, i<25,i++){
    random number generator for grid.unusedSpot
    grid.fillBox()
  }
if== level easy
  for (int i =0, i<30,i++){
    random number generator for grid.unusedSpot
    grid.fillBox()
  }
return gird.fillBox()
